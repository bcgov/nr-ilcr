# Classpath JasperReports config (AD-13). A missing glyph must never throw during a headless PDF
# fill on the read-only-root OpenShift image; degrade to a substitute instead. Bundled fonts
# (jasperreports-fonts) cover the Latin text this report renders.
net.sf.jasperreports.awt.ignore.missing.font=true
