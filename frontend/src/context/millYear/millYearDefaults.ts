// Dev/UAT default mill + reporting-year context (spine test defaults). These live here — not
// hardcoded in the page component — because the scaffold has no runtime/env-config layer yet
// (same status as the mock-auth scaffold). A real runtime-config injection is deferred.
export const DEFAULT_MILL_ID = 514
export const DEFAULT_YEAR = 2021
